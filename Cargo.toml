[workspace]
resolver = "3"
members = ["crates/game", "crates/networking"]
default-members = ["crates/game"]

[workspace.dependencies]
networking = { path = "crates/networking", version = "0.1.0" }

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
